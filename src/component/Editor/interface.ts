export interface ICSSParameter {
  name: string;
  value: string;
}

export interface ICategoriesStyle {
  size: {
    width: string,
    height: string,
  },
  color: {
    color: string,
    backgroundColor: string,
  }
}